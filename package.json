{
  "name": "pcc-richtext",
  "private": true,
  "version": "1.0.0",
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "scripts": {
    "build": "node scripts/build.mjs && node scripts/build-readme.mjs"
  }
}
