{
  "name": "pcc-richtext",
  "private": true,
  "version": "0.9.1",
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "scripts": {
    "build": "node scripts/build.mjs && node scripts/build-readme.mjs"
  }
}
